# 기본 의존성 (최소 필수 패키지만)
eripotter-common==0.1.4
uvicorn[standard]==0.24.0
psycopg2-binary==2.9.9
alembic==1.13.1
python-multipart==0.0.9
python-dotenv==1.0.0

# RAG 관련 패키지 (필수만 포함, 버전 고정으로 빌드 시간 단축)
qdrant-client==1.15.1
openai==1.12.0
langchain==0.1.20
langchain-community>=0.0.38,<0.1
langchain-openai==0.0.8

# 빌드 시간 단축을 위해 불필요한 패키지 제거
# - sentence-transformers: 매우 큰 패키지 (약 500MB+)로 빌드 시간 증가
# - 기본값: EMBEDDER=openai 사용 (sentence_transformers 불필요)
# - 필요시: EMBEDDER=bge-m3 또는 EMBEDDER=minilm 환경변수 설정 시 자동 fallback
# - 설치: pip install sentence-transformers==5.1.0